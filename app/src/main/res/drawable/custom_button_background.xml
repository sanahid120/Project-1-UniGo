<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed State -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#FF8A65" /> <!-- Lighter orange for pressed state -->
            <corners android:radius="16dp" />
            <stroke android:color="#FF7043" android:width="2dp" /> <!-- Add border effect -->
        </shape>
    </item>

    <!-- Default State -->
    <item>
        <shape>
            <gradient

            android:angle="45" />
            <corners android:radius="16dp" />
            <stroke android:color="#277FC1" android:width="3dp" /> <!-- Slight border contrast -->
        </shape>
    </item>
</selector>
